extends ../layout

block content
  h1= title
  p 
    | Are you sure you want to delete 
    em= user.title
    | ?
  form(action="/users/" + user.id + "?_method=DELETE", method="post")
    a(href="/users/" + user.id) No! Take me back!
    | 
    input(type="submit", value = "Yes, I am")